import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['media.graphassets.com', 'us-west-2.graphassets.com'], // Add the required domain here
  },
};

export default nextConfig;
